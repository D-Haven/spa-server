apiVersion: v1
kind: Service
metadata:
  name: {{ include "spa-server.fullname" . }}
  labels:
    {{- include "spa-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spa-server.selectorLabels" . | nindent 4 }}
