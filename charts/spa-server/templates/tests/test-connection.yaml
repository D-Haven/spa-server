apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spa-server.fullname" . }}-test-connection"
  labels:
    {{- include "spa-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spa-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
